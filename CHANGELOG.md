# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，从 `0.2.0` 开始在此记录面向用户的变化。

## 0.3.2 - 2026-08-12

### 新增

- 归档会话与快照支持恢复、自动备份和归档范围展示，避免归档数据混入当前会话。
- 会话详情支持按当前会话精确查找关联的 Codex、Claude Code 和 OpenCode 进程；Codex 可在确认后关闭进程树。
- 设置页支持刷新本地模型价格，包含 `claude-fable-5`，并提供联网失败时的内置价格兜底。

### 改进

- 价格按规范化后的模型前缀匹配，厂商前缀和版本号分隔符可兼容；旧的裸模型规则会自动迁移。
- WSL 设置仅在 Windows 显示；macOS 和 Linux 不再展示无效的 WSL 控件。
- Claude Code、Codex 和 OpenCode 的会话来源、归档和详情读取路径更加一致。

## 0.3.1 - 2026-08-11

### 新增

- macOS Apple Silicon 版本现在支持应用内在线更新。Release 会同时提供 DMG、签名的
  `.app.tar.gz` updater 包，并在 `latest.json` 中发布 `darwin-aarch64` 更新信息。

### 改进

- Windows x64 和 macOS arm64 安装包改由 GitHub Actions 自动测试、签名和发布；任一
  平台失败时不会发布不完整的 Release。

## 0.3.0 - 2026-08-08

### 新增

- 支持读取 WSL 发行版里的 Claude Code / Codex / OpenCode 数据。WSL 中的安装会并入原有的三个数据源，
  会话、项目和快照上以 `WSL:<发行版>` 标签区分；从会话打开终端或恢复会话时会用 `wsl.exe` 在对应发行版
  内启动。可在设置页关闭扫描或排除指定发行版（首次读取发行版共享目录会启动该发行版）。

### 改进

- 大幅加快快照的创建与恢复前自动备份：同卷改用目录移动（原地重命名）代替逐文件复制，并去掉了创建过程中
  的两次额外全目录遍历；跨卷时的复制与清理改为并行执行。实测 4 GB / 480 个会话文件的快照创建从约 3.7 秒
  降到 9 毫秒。恢复仍为复制（归档必须保留），耗时不变。

## 0.2.3 - 2026-07-29

### 修复

- 修复按日用量柱形方向异常，以及悬浮最右侧柱形时提示卡片导致统计容器出现滚动条的问题。

## 0.2.2 - 2026-07-29

### 改进

- 用量统计的按日柱状图新增悬浮汇总卡片，可查看每天的精确 Token 总数和会话数，并支持键盘聚焦查看。

## 0.2.1 - 2026-07-29

### 修复

- 设置页的更新日志现在会按 Markdown 正确渲染标题、列表、链接和代码块，不再直接显示 Markdown 源文本。

## 0.2.0 - 2026-07-28

### 新增

- 新增 Claude Code、Codex、OpenCode 的统一会话管理，支持查看完整会话、精确搜索 Session ID、复制 ID 和恢复会话。
- 会话列表新增 Agent 生成的标题/摘要、创建时间、最后交互时间和模型信息，不再只向用户展示 Session ID。
- 工具、指令、记忆等页面按 Agent 来源区分，并支持 Codex `Instruction.md` 与记忆文件。
- 新增搜索索引状态与构建提示；索引完成后跨 Agent 会话、搜索、摘要和最后交互时间才会完整可用。
- 设置页新增应用版本、更新日志和在线更新入口，更新包下载后可直接安装并重启。

### 改进

- 会话列表改为使用完整索引数据，保留标题、摘要、时间、模型、恢复和筛选能力。
- 优化 Claude Code 会话索引的重复扫描、活跃会话写入抖动以及后台索引 CPU 占用。
- 修复 Codex 记忆标题乱码及无法从关联会话读取可读标题/摘要的问题。
- 终端工具输出会清理 ANSI/VT 控制序列，PowerShell 彩色表格不再显示为 `[32;1m` 等碎片。
- 统一将“交互”文案明确为“最后交互”。

### 修复

- 修复 Claude Code 会话页面一直停留在“加载中”的前端渲染异常。
- 修复会话标题缺失时调用 `trim()` 导致整个列表渲染失败的问题。
